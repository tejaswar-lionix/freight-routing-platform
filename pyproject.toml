[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[project]
name = "freight-routing-platform"
version = "1.0.0"
description = "Freight Routing Platform"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Proprietary"}
